How to extend it later:

Add richer OpenAPI diffing for operation IDs, tags, parameters, and request bodies.
Add service-to-doc ownership rules so docs can be mapped to specific services instead of only using global stale detection.
Add test coverage and CI checks that fail builds when drift crosses a configurable threshold.
Add output adapters for SARIF, CSV, or GitHub Actions annotations.